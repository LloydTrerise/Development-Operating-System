# DEVOS-065 — Implement Review Agent

**Dependencies:** DEVOS-064 (test evidence). **Scope:** "Review diff against PRD/design/standards" (source description) — Stage 9 — Engineering Review (§19): primary agent is the "Code Review Agent"; inputs are "approved requirements; technical design; implementation plan; code change; validation evidence; engineering standards"; findings are classified `BLOCKER`/`MAJOR`/`MINOR`/`NOTE`; outcome is `PASS` or `CHANGES_REQUIRED`. A new `review-agent` `Agent`/`AgentVersion` (`role: 'REVIEW'`) — a normal agent definition matching DEVOS-031–034/057's exact precedent, not a new domain entity. New prompt and output schema under `packages/agents/src/schemas/` (not `packages/artifacts` — see this sprint's README for why that package stays an unactivated scaffold, consistent with Sprints 1–4). **"Engineering standards" has no concrete source anywhere in the spec corpus** — no dedicated "coding standards" artifact/knowledge-source type exists; satisfied by whatever `ACTIVE` project knowledge sources are already retrievable (DEVOS-040's `retrieveActiveKnowledgeSources`), not a fabricated new input type. **Acceptance:** given the `PRD`/`TECHNICAL_DESIGN`/`IMPLEMENTATION_PLAN`/`CODE_CHANGE`/`TEST_EVIDENCE` artifacts for a work item, the review agent produces a schema-validated output containing a findings list (each classified `BLOCKER`/`MAJOR`/`MINOR`/`NOTE`) and an overall `PASS`/`CHANGES_REQUIRED` decision.
