# DEVOS-016 — Deterministic Task Stub

**Dependencies:** DEVOS-014, DEVOS-015. **Scope:** Workflow task emits a deterministic `DISCOVERY_REPORT` artifact and structured successful output, without an LLM; replaceable by an agent handler. **Acceptance:** Runs through normal task machinery and publishes artifact.
