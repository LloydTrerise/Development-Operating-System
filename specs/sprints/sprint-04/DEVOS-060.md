# DEVOS-060 — Development UI

**Dependencies:** DEVOS-057 (development agent), DEVOS-058 (PR creation). **Scope:** Show workspace, diff, and PR evidence (source description). No `development`/`workspace`/`diff` feature folder is listed anywhere in `specs/architecture/repository-code-structure.md` §10's `apps/web/src/features/` tree (only `projects`/`work-items`/`workflows`/`runs`/`artifacts`/`approvals`/`agents` exist) — no wireframe, read-API contract, or "diff view" concept is specified anywhere. The only anchor is Stage 7's output list: "code change; commit evidence; pull request where applicable; implementation evidence" (`specs/workflows/software-change-workflow.md` §17). **Acceptance:** Mirroring the established `apps/web/src/pages/*.tsx` pattern (not a new `features/` layout, consistent with every prior sprint's UI work in this codebase), a reviewer can see, for a development task: the tool invocations it produced, their outcome, and the resulting commit/PR evidence — an implementation-level UI shape, since none is specified.
