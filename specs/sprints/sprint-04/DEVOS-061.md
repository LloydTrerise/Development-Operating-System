# DEVOS-061 — Development E2E

**Dependencies:** DEVOS-051 through DEVOS-060. **Scope:** Approved plan through code/PR evidence (source description) — the exact slice `specs/api/poc-api-contracts.md`'s reference run sequence names (steps 10–16): "Create approval... Pause in AWAITING_APPROVAL... Human approves... Resume development task... Tool gateway creates branch/commit/PR... Run build/tests... Review agent evaluates evidence." This sprint's slice covers approval through PR evidence; build/test/review are Sprint 5 scope. No dedicated `PullRequestCreated`/`CodeChangeCreated` workflow event exists anywhere (only the generic `ToolInvocationCompleted`) — flagged rather than invented. **Acceptance:** Extending this repository's established real-process E2E pattern (`tests/e2e/planning-path.test.ts`), a single automated test demonstrates: an approved planning package (DEVOS-050's own proof) leads into a development task, a controlled workspace is created, the development agent proposes a change, the Tool Gateway applies it (branch + commit) and creates a PR record through the fake/local provider (per this sprint's live-verification scoping decision), all tool invocations are authorised/idempotent/audited, and the resulting evidence (commit, PR record) is retrievable via the API.
