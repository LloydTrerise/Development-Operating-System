# DEVOS-050 — Planning Approval E2E

**Dependencies:** DEVOS-039 through DEVOS-049; extends Sprint 2's `tests/e2e/planning-path.test.ts` (DEVOS-038). **Scope:** Extend the automated planning-path proof to the exact POC acceptance scenario in `specs/workflows/software-change-workflow.md` §45 — Discovery → PRD → Technical Design → Implementation Plan → Human Planning Approval — through the real API/worker processes against real Postgres, with no live model call (fixture adapter, per DEVOS-038's established pattern). **Acceptance:** A single automated test demonstrates the full chain: a work item produces all four planning artifacts with context manifests/provenance recorded (DEVOS-041/042), the run reaches `AWAITING_APPROVAL` (DEVOS-047) bound to those artifacts via a scope hash (DEVOS-045), an approval decision is recorded and auditable, and the run's audit trail reflects the complete, evidence-backed lifecycle — matching the rigor of DEVOS-038's own proof for Sprint 2.
