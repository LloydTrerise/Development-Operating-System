# DEVOS-025 — Implement Agent Definition Model

**Dependencies:** Sprint 1 (`packages/domain`/`packages/application` contracts, DEVOS-011's versioned-entity pattern). **Scope:** Versioned agent definitions and their capabilities, modeled in `packages/domain`/`packages/application` using existing contract conventions rather than a parallel model. **Acceptance:** An agent definition can be created, versioned, and queried through the application layer, with tests covering the definition/version lifecycle; no existing Sprint 1 acceptance criteria regress.
