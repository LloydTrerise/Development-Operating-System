# DEVOS-032 — Implement Requirements Agent

**Dependencies:** DEVOS-031. **Scope:** Generates a validated PRD from the discovery agent's report — the second stage of the planning path. **Acceptance:** Given a discovery report, the requirements agent produces a schema-validated PRD artifact, correctly linked (via provenance and context manifest) to the discovery report it was derived from.
